Overlay files for a future standard-library port live here.

The current workspace uses the local `mochi-user-*` crates as the immediate
userland substrate, while upstream `rust-src` remains untouched.
