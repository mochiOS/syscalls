[package]
name = "rust-std-demo"
version = "0.1.0"
edition = "2024"

[workspace]

[patch.crates-io]
libc = { git = "https://github.com/mochiOS/libc", package = "libc" }

[profile.dev]
panic = "abort"

[profile.release]
panic = "abort"
