[package]
name = "mochi-user-platform"
version = "0.1.0"
edition = "2024"

[dependencies]
mnu-abi = { path = "../../../core/crates/abi" }
mochi-user-syscall = { path = "../syscall" }
mochi-user-runtime = { path = "../runtime" }
mochios-capability-protocol = { path = "../capability-protocol", default-features = false }

[features]
default = []
